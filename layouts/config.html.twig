{% set confg_menu =  [
    {
        title: 'App Configuration',
        href: '#app-configuration'
    },
    {
        title: 'Database',
        href: '#database'
    }
] %}

{# Template #}
<div class="">
	<nav>
		<ul>
			{% for item in confg_menu %}
				<li>
					<a href="{{item.href}}">{{item.title}}</a>
				</li>
			{% endfor %}
		</ul>
	</nav>
	<div id="config-generator-content"></div>
</div>
