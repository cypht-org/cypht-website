title: "Cypht Website"
baseline: "cypht.org website"
baseurl: "https://www.cypht.org/"
description: "Cypht is a lightweight, secure, open-source webmail client that aggregates multiple email accounts and services into a single unified interface."
author:
  name: multiple
  url: "https://example.com/"
image: cecil-card.png

# Reusable metadata (used by JSON-LD, OG tags, footer)
organization:
  name: "Cypht"
  logo: "img/logo.svg"
  github: "https://github.com/cypht-org/cypht"
  license: "https://github.com/cypht-org/cypht/blob/master/LICENSE"
software:
  category: "Webmail"
  os: "Cross-platform, self-hosted (PHP)"

pages:
  virtual:
    - path: contribute-redirect
      slug: contribute
      redirect: /contribute/
      uglyurl: true
    - path: cypht-enable-gmail-oauth-redirect
      slug: cypht-enable-gmail-oauth
      redirect: "/documentation/account-setup/#gmail_oauth"
      uglyurl: true
    - path: cypht-enable-outlook-and-office365-oauth-redirect
      slug: cypht-enable-outlook-and-office365-oauth
      redirect: "/documentation/account-setup/#outlook_oauth"
      uglyurl: true
    - path: developers-documentation-redirect
      slug: developers-documentation
      redirect: /documentation/dev-docs/
      uglyurl: true
    - path: documentation-redirect
      slug: documentation
      redirect: /documentation/
      uglyurl: true
    - path: email-filters-redirect
      slug: email-filters
      redirect: /email-filters/
      uglyurl: true
    - path: features-redirect
      slug: features
      redirect: /features/
      uglyurl: true
    - path: how-to-join-redirect
      slug: how-to-join
      redirect: /how-to-join/
      uglyurl: true
    - path: install-redirect
      slug: install
      redirect: /install/
      uglyurl: true
    - path: install-1x-redirect
      slug: install-1x
      redirect: /install/
      uglyurl: true
    - path: install-2x-redirect
      slug: install-2x
      redirect: /install/
      uglyurl: true
    - path: license-redirect
      slug: license
      redirect: /license/
      uglyurl: true
    - path: modules-redirect
      slug: modules
      redirect: /modules/
      uglyurl: true
    - path: security-redirect
      slug: security
      redirect: /security/
      uglyurl: true
    - path: tests-redirect
      slug: tests
      redirect: /tests/
      uglyurl: true
